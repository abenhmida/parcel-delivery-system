server:
  port: 8080

spring:
  datasource:
    password: parcel
    username: parcel
    url: jdbc:postgresql://localhost:5432/parcel_delivery

  flyway:
    enabled: true
    locations:
      - classpath:db/migration